
  # --- AZZLE protocol (Base) — added by npx @azzle/agents aeon-setup ---
  azzle-market: { enabled: false, schedule: "0 8 * * *", var: "" } # daily POSTED-task digest from subgraph
  azzle-worker: { enabled: false, schedule: "workflow_dispatch", var: "" } # on-demand worker playbook; var=taskId or focus
